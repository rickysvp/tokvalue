# TokValue SEO 强化 — Step 1 技术地基审计与修复（2026-08-16）

## 目标
逐个加强 SEO。第 1 步：审计并修复技术 SEO 地基（只读审计 → 用户确认 P0=A → 改代码）。

## 审计结论：地基已较扎实
- ✅ `app/sitemap.ts`：静态页 + 博客文章（动态）+ 作者 + 分类 + 标签（≥2 篇才收）全覆盖
- ✅ `app/robots.ts`：放行 + 指向 sitemap
- ✅ `HomepageJsonLd`：`WebSite`+`Organization`+`WebApplication`(AggregateOffer/isAccessibleForFree)+`FAQPage`
- ✅ `BlogPostJsonLd`：`BlogPosting`+作者`Person`+`publisher`+可选`FAQPage`
- ✅ 多数页面有 metadata + canonical + openGraph + Twitter 卡

## 决策（用户确认）
- **P0 = A**：`/evaluate` 与报告页（`/share/[id]`，原以为在 `/r/[id]`，实际 `/r` 不存在）**不收录**（`noindex, follow`）。搜索红利靠 Pillar 页 + 博客 + 基准页承接，规避薄内容/第三方账号 PII 风险。
- P1/P2 一并按推荐修（低风险打磨）。

## 改动清单（7 文件，+4 新增 layout）
1. `app/layout.tsx`：title 改为 `{ default: 'TokValue', template: '%s · TokValue' }` —— 子页自动带品牌后缀，解决博客标题光秃秃问题
2. `app/page.tsx`：首页 title 改 `absolute: d.seo.title` —— 避免与 template 重复品牌
3. `app/evaluate/[username]/page.tsx`：加 `robots:{index:false,follow:true}`；title 去掉 "— TokValue"（交给 template）
4. `app/share/[id]/layout.tsx`（**新建**）：client 页不能 export metadata，用 layout 层加 `noindex, follow`
5. `app/blog/category/[slug]/page.tsx`：generateMetadata 补 `alternates.canonical`
6. `app/blog/tag/[tag]/page.tsx`：generateMetadata 补 `alternates.canonical`（薄 tag 页原有 noindex 逻辑保留）
7. `app/robots.ts`：加 `disallow: ['/api/']`

## 已确认无需改
- `app/tiktokmaster/layout.tsx`：已是 `noindex, nofollow`（管理员登录页，正确排除）
- 博客 category/tag 原有 noindex 薄页逻辑保留

## 验证
- `npx tsc --noEmit` → exit 0

## 下一步（Step 2）
- 建 "TikTok Money Calculator" Pillar 页（内嵌评估工具 + FAQ schema + 基准/解释内容），作为核心排名页
- 待用户确认方案后再建
